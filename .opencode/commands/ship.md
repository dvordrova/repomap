---
description: Publish the accepted current decision safely
agent: workflow-manager
---

This invocation explicitly authorizes a normal push of the current branch.

Verify fresh acceptance PASS for the active decision and current code state, run required
checks, mark the decision implemented, commit coherent remaining accepted changes, and
push normally.

Never force-push, rewrite history, create tags, merge, or push an ambiguous branch.
