FINAL RESPONSE FORMAT: Return the response_example shape below: result FIRST and terms LAST as outer siblings, never terms inside result. The owner-supplied result example specifies its exact JSON type, containers and fields; retain objects around their arrays and do not add wrappers or encode result as a string. Compute the ANSWER to the original task, never copy its REQUEST, input table, fill schema or evidence catalogue. Include every required answer item; illustrative values and array lengths are not decisions or quotas. Add only the supported name/explanation/sources definitions described above, or terms: [].
