# Response format

Return exactly one JSON value with the following shape, without Markdown or
surrounding prose. Compute the task result; do not copy its input or this
example's illustrative values. Array lengths in the example are not quotas.
