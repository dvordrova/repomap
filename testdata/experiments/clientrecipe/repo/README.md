# Launch service

Historical setup notes say external connections are created in `cmd/service`.
That is stale: the live composition root is `internal/app`.

The retired legacy client is also described here as active, although no live
bootstrap path calls it. Documentation alone is not production reachability.
