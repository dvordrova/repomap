[metadata]
name = repomap-cumulative-python-fixture

[options]
packages = find:
package_dir =
    = src

[options.packages.find]
where = src
include =
    fixture_app*
    acme
    acme.*
exclude =
    acme.excluded
    acme.excluded.*
