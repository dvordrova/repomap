[project]
name = "repomap-cumulative-python-fixture"
version = "0.0.0"
dependencies = [
  "fastapi",
  "httpx",
  "kafka-python",
  "uvicorn",
]

[project.scripts]
repomap-fixture = "fixture_app.cli:main"
