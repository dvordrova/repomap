# Documentation split receipt — non-normative

This records the 2026-09-11 documentation edit. It is not a second implementation
contract. Current work starts at [AGENTS](../../../AGENTS.md), then
[CURRENT](../../agent-room/CURRENT.md) and the relevant topic only.

The original bodies were preserved byte-for-byte beneath an explicit history
banner:

| Original | Lines | SHA-256 of original body |
| --- | ---: | --- |
| AGENTS | 1,044 | `7f71f5207bf03ffda28ba40604d6e346a63f672f4e0576d770edb98521c37178` |
| CURRENT | 5,607 | `5998d6991657e36818b4a2112f8d4e2ec7af12d9be8013cfb088918686978c77` |

Current detail was transferred by topic, not by date. The original CURRENT
explicitly labels the old categorization, grouping and group-matching sections
as deleted history under “Atlas (the model path, from 2026-09-04)”. Those stages,
the retired `--atlas` mode, historical numeric schema generations and their
acceptance measurements remain only in the snapshots. Current GroupsIndex is
still the atlas projection. Old mandatory `u*` operation handoffs and inline
`result/terms` designs are superseded by the current implementation contracts.

Contradictions were resolved against existing current decisions and owning code:

- The corpus excludes dependency/cache roots, while `build`, `dist` and
  `coverage` names alone do not exclude source (`internal/corpus`).
- No credential scanner or freshness gate was restored. The constitution and
  existing trusted-repository decision supersede old scanner paragraphs.
- Former local scale thresholds do not authorize warnings, sampling or
  truncation; actual provider envelopes and explicit narrowing keep their scope.
- Source observations and semantic runtime roles stay separate. Platform
  package objects are not remote participants; a source call through a platform
  transport can support communication. Dynamic execution is not automatically
  an incoming operation.
- Shared 128,000-token output allocation includes the separate glossary.
  Provider messages have one owning response shape; prose/source context stays
  local through cache, memo and replay.
- New native evidence, operation/boundary contracts, per-anchor question
  relevance and Data scope rules were incorporated from the current wave.
  Saved probes and local checks were not promoted to full ordinary acceptance.

Repeated historical tests/runs, abandoned alternatives and one-off measurements
remain available in the snapshots. Their removal from the active topic text is
not a reversal of any accepted product invariant. Current interpretation limits,
unknown frontiers and counterexamples remain with their owning contract.

Required cumulative native fixtures, exact file inventories, cross-language
examples, real extractor execution, consuming expectations, test/vet bounds,
owner binary build, ordinary online publication checks, browser journeys and
cache/warm/clear checks remain required. Constitution text was unchanged.

Existing ATLAS_PLAN, HANDOFF and NIGHT-BRIEF paths remain intact with explicit
history notices; historical line numbers are no longer presented as current
line addresses. User review documents and unrelated untracked files were not
removed or rewritten. The [topic mapping](topic-mapping.json) lists the final
routes, and the live journal records only current correction/check evidence.
