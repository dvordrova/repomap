# Decision: Canonical Study Ordinary-Publication Corrective

Status: Active. Approved by the repository owner in the current session after
one ordinary provider-backed etcd run and screenshot-backed product-supervisor
review.

## Baseline

- commit: `1ddfe25a`;
- diagnostic run:
  `/Users/dvordrova/Library/Caches/repomap/runs/20260730-052459-etcd-95f29c38dab0`;
- preserve all unrelated untracked Decision 140, semantic-map, and Python-focus
  experiment files.

## Observed failure

The ordinary etcd run completed local runtime discovery in about 25 seconds,
but spent about 101 seconds on semantic opportunity discovery and about
23 seconds on paved-path editing. Both stages published zero primary content.
The canonical Study reducer selected six directions, while report replay and
presentation exposed all eight pre-reduction candidates and three locally
ineligible semantic topics. Ordinary publication therefore paid for rejected
editors and bypassed its accepted canonical state.

## Approved corrective scope

1. A non-empty canonical saved Study Map is the only Study publication
   projection. Pre-reduction incomplete directions and locally ineligible
   semantic topics remain absent when that canonical projection exists.
   Incomplete Study remains the exact-start fallback only when no canonical
   Study Map was accepted.
2. Overview and Study render the same ordered canonical Study directions.
   Existing exact-source navigation and attached Mechanism routing remain
   unchanged.
3. Ordinary `repomap` no longer eagerly schedules semantic opportunity
   discovery or paved-path editing. Their explicit developer entrypoints and
   saved-artifact replay remain available.
4. Architecture synthesis, evidence-producing targeted research, canonical
   Study proposal/reduction, local graph discovery, freshness, and report
   publication remain unchanged.

## Non-goals

- no new provider request, request shape, language adapter, Search surface,
  proof semantic, timeout policy, budget target, report format, or broad UI
  redesign;
- no deletion of canonical semantic or paved-path artifact support;
- no provider call required for the corrective regression.

## Acceptance

- a provider-free regression shaped from the saved etcd run proves that the
  same six reducer-selected directions render on Overview and Study, while the
  two rejected proposals and three ineligible semantic topics are absent;
- an ordinary-run request-plan regression proves semantic opportunity and
  paved-path editors are not scheduled;
- focused tests, `./scripts/check.sh`, and `./scripts/etcd_check.sh ../etcd`
  pass before rebuilding the single stable PATH binary;
- saved-artifact UI replay and product-supervisor screenshots confirm the
  corrected ordinary experience before any fresh provider-backed matrix run.
