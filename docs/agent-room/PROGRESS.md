# Progress

## On now
Nothing in flight. Reading the three reports, auditing their claims against
the repositories, and reviewing the night's own diff. Owner's redirection
governs: visual hierarchy, a canvas graph, one self-contained HTML.

## Finished
1. **Instruments.** Categorization coverage with denominators, grouping shape,
   and the size of a request plan before it runs.
2. **Findings settled.** The 172 non-object assignments are relation patterns.
   `core` is not worth narrowing; what finding 1 pointed at was a parser
   refusing a repeated key and a prompt with no size rule.
3. **Speed and coverage.** Fixture 5.6 s warm (from 14.6 s), 2/2 targets. chi
   7.7 s, 4/4. python-dotenv 19.5 s, 3/3. Two consecutive runs of the same
   repository now produce a byte-identical report.
4. **The page.** Templates one file per region and per layer. Each target
   opens with an SVG map of its groups, sized by membership, marked with the
   main-flow steps through them, and saying how much of the target it covers;
   a repository map; summary first, quotes last; the flow as a numbered
   sequence; sticky nav and per-target jump bars. 131–236 KB, 5.2 KB script,
   nothing external, no overflow at 375 px.
5. **Truth, audited not assumed.** 658 anchors across the three reports all
   resolve to a real file and line; 41 text-bearing facts and 37 route paths
   match their source. Two claims were wrong and are fixed: `APP_PORT` has a
   default of 8080, and a Python package's `__init__.py` is not dead code when
   the entrypoint imports through it. Group sizes have a rule; mounted route
   prefixes compose; listen addresses are facts; a README is quoted as written.
6. **Robustness.** A provider timeout is retried. A failed merge keeps the
   shards' groups. Two targets in one directory no longer collide. The default
   command works with no editor installed.

## Stuck on
Nothing. In the handoff, recorded not fixed: the merge phase should ask which
candidates belong together instead of asking for a copy; the index has no
value for numeric literals; `os.environ["KEY"]` reads; whether tests belong on
the map; beets is past medium; fuego fails on incomplete `go list`.

## Next
Keep reading and auditing.
