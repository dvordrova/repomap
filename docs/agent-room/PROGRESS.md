# Progress

## On now
Nothing in flight. Reading the three reports and fixing what is hard, on the
owner's redirection: visual hierarchy, a canvas graph, one self-contained
HTML. Until 11:00.

## Finished
1. **Instruments.** Categorization coverage with denominators, grouping shape,
   and the size of a request plan before it runs — all on every run.
2. **Findings settled.** The 172 non-object assignments are relation patterns,
   0 invented. `core` is not worth narrowing. What finding 1 pointed at was a
   parser refusing a repeated key and a prompt with no size rule; both fixed.
3. **Speed.** Fixture 14.6 s → 7.1 s warm, 55 s cold. chi 8.2 s warm, 111 s
   cold, 4/4. dotenv 19.8 s warm, 3/3. Biggest win: the run id sat in the
   facts digest, so orientation never hit its cache. Reports are now
   byte-identical across runs.
4. **The page.** Templates one file per region and per layer. Every target
   opens with an SVG map of its groups, sized by membership, marked with the
   main-flow steps that pass through them; a repository map; summary first and
   quotes last; the flow as a numbered sequence; sticky nav and a per-target
   jump bar with counts. 128–234 KB, 5.2 KB of script, nothing external.
5. **Truth.** A repeated identical key no longer discards whole answers.
   Mounted route prefixes compose. Listen addresses are facts. Group sizes
   have a rule: the same repository gave 4 to 13 groups with two thirds of a
   target in one box, now 8–13 with the largest at 4%. The main flow reaches
   the response. A README is quoted as what it says, badges and all.
6. **Robustness.** A provider timeout is retried. A failed merge keeps the
   shards' groups instead of losing a target. Two targets in one directory no
   longer collide. `repomap <path>` works with no editor installed.

## Stuck on
Nothing. Recorded in the handoff, not fixed: the merge phase asks the model to
copy thousands of refs and should instead ask which candidates belong
together; whether tests belong on the map is unanswered; beets is far past
medium; fuego fails on incomplete `go list` authority.

## Next
Keep reading the three reports and fixing what is hard.
