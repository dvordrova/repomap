# MORNING-231 — Archive 9 semantic-product program, Program 2

**Date:** 2026-08-07 (live matrix: 2026-08-07)
**Status:** COMPLETE (PASS after four bounded decisions + repairs)

## Program

Continue Repomap from the owner-supplied Archive 9 evidence and
independently complete the next semantic-contract and product-value
program. Four bounded numbered decisions (231–234), local commits only
(no push), full authorization for bounded decisions/checkpoints/fresh
reviews/repairs. Single terminal `HERMES PASS/HOLD/BLOCKED`.

## Decisions

| # | Decision | Commit | Verdicts |
|---|---|---|---|
| 231 | Architecture model/backend boundary — shared participation over type-blind slicing; ref kind backend-owned | `bb83d12` + gap closure `11c0040` | + repairs (miniflux gap) |
| 232 | Navigator v2 (model selects action only; backend restores trail/endpoints/evidence) + Theme Scout/Adjudication contract simplification | `99a32fa` | 11/11 fresh-review verdicts applied |
| 233 | Study portfolio equivalence (co-projection, generic concentration) + Overview entry-surface prominence + AREA COVERAGE | `1d31449` + `3af9431` | D1–D10 + F1–F8 all applied |
| 234 | Canvas pointer-local wheel contract (owner corrective 1) + TLS-family dominance bias (owner corrective 3) | `08ac19f` | PASS + F1 required applied |

## Owner correctives

1. **Canvas** — wheel over the map zooms (canvas owns wheel/trackpad over
   the map, one documented behavior; supersedes D230 D3); blank-drag pan;
   +/−/Fit/reset work; persistent quiet hint «Колесо — масштаб ·
   перетаскивание — перемещение» (role=note + aria-label); Fit exposes
   every principal node (upper-only clamp — no lower Fit floor); state
   deterministic; no occlusion; inspector/source preserve transform;
   no acceptance blocker raised for wheel ownership.
2. **Entry surfaces** — repository-shape + product-role projection in
   Overview: primary process entries, CLI tree (cli_tool/daemon shapes),
   HTTP/API handlers (service/app), public API (library), workers/jobs
   (async), monorepo per-app summaries; categories with exact totals,
   1–3 representatives, Show-all-N, unresolved count separate; value-shaped
   entries counted under bounded disclosure; telebot library role survives.
3. **TLS/security-family dominance** — bias, not truth: 4 stages, no fact
   removal; casdoor keeps all 9 TLS anchors + 295 certificate mentions,
   Study shelf not TLS-dominated (11 themes, 0 TLS); generic concentration
   diagnostic (no TLS string in the rule); d234_tls_bias test proves shared
   participation never claims package observations.

## Evidence

- Replay: 5/5 Archive 9 fixtures PASS provider-free (casdoor accepted,
  etcd accepted_partial, telebot accepted with roles, chatto accepted,
  restic accepted_partial) — byte-equal canonical records.
- make check: 0 FAIL (all packages + go vet + node checks).
- Live matrix: single fresh run of 5 fixtures (see live-matrix/).
- Durable run: tmp/hermes-archive9-semantic-product-20260806-182649/
  (BASELINE, STATE, RUN_LOG, PROMPT_VALIDATOR_MATRIX,
  FINAL_PROMPT_VALIDATOR_MATRIX, ARCHIVE9_MATRIX,
  INFORMATION_RETENTION_BASELINE/FINAL, ARCHITECTURE_SHARED_PARTICIPATION,
  STUDY_PORTFOLIO, ASSOCIATION_AND_MECHANISM, BROWSER_ACCEPTANCE).

## Repairs during the program

- **D231 gap closure** (miniflux live run 20260806-223340): member_refs may
  omit the backend-owned kind — decode required kind+ref and whole-rejected
  a valid proposal; fixed in `11c0040` (ref-only catalog resolution,
  omitempty wire, regression tests).
- **D233 AREA COVERAGE** (goal PHASE 4): exact missing-core-area
  diagnostic in Study status + UI (projection 11→12, format 34→35).

## Information retention

All TLS/certificate facts retained; no valid theme/reading/anchor/surface
disappears; suppressed/co-projected anchors reachable and counted;
missing-core-area diagnostic adds visibility. Reduced false prominence
(shape-aware Overview, TLS bias) and false ownership (shared participation,
kind-less refs) — see INFORMATION_RETENTION_FINAL.md.

## Terminal verdict

**HERMES PASS**
